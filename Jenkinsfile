def GV
pipeline{
  agent any
  stages{
    stage('init'){
      steps{
        echo "enter init stage commands"
      }
    }
    stage("build"){
      steps{
        echo "building.."
      }
    }
  }
}
